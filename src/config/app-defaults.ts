/**
 * Canonical defaults shared by layout cookie fallbacks and i18n (`common.app` via `i18n/request.ts`).
 * Update values here only; do not duplicate in locale JSON.
 */
export const DEFAULT_ULB_CODE = 'TMC';
export const DEFAULT_ULB_NAME = 'Sthapatya Consultant (I) Pvt.Ltd';
